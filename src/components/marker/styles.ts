import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  markerFixed: {
    left: '50%',
    top: '50%',
    marginLeft: -24,
    marginTop: -36,
    position: 'absolute',
  },
  marker: {
    height: 48,
    width: 48,
  },
});

export default styles;
